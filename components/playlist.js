import styles from '../styles/playlist.module.sass'

const Playlist = () => {
    return(
        <div className={styles.playlist}>
            <h1>Playlist</h1>
        </div>
    )
}

export default Playlist
